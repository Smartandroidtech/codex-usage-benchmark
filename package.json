{
  "name": "codex-window-benchmark-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/client dist/server dist/.openai && cp -R site/. dist/client/ && cp hosting/server.js dist/server/index.js && cp .openai/hosting.json dist/.openai/hosting.json"
  }
}

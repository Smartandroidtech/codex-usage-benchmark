Read only input.json using one shell command; do not list or search other files. For each region compute total quantity, total revenue in cents, and the SKU with the largest revenue (ties: lexicographically smallest SKU). Return only JSON matching the supplied schema. task_id must be "aggregate-v1". answer must contain exactly three strings, sorted by region, formatted REGION|QTY|REVENUE|TOP_SKU.

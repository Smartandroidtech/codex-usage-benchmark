const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('#main-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});
const tooltip=document.querySelector('.tooltip');const tipTargets=document.querySelectorAll('[data-tip]');function placeTip(target){const r=target.getBoundingClientRect();const w=Math.min(260,innerWidth-24);let left=Math.max(12,Math.min(r.left+r.width/2-w/2,innerWidth-w-12));let top=r.bottom+10;if(top+100>innerHeight)top=r.top-90;tooltip.style.width=w+'px';tooltip.style.left=left+'px';tooltip.style.top=top+'px'}function showTip(e){tooltip.textContent=e.currentTarget.dataset.tip;tooltip.classList.add('visible');tooltip.setAttribute('aria-hidden','false');placeTip(e.currentTarget)}function hideTip(){tooltip.classList.remove('visible');tooltip.setAttribute('aria-hidden','true')}tipTargets.forEach(el=>{el.addEventListener('mouseenter',showTip);el.addEventListener('mouseleave',hideTip);el.addEventListener('focus',showTip);el.addEventListener('blur',hideTip)});addEventListener('scroll',hideTip,{passive:true});addEventListener('resize',hideTip);
